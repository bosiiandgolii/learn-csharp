using System;

class Program
{
    static void Main()
    {
        // == != > < >= <=

        int age = 18;
        bool acceptInvite;

        acceptInvite = age >= 18;
        System.Console.WriteLine(acceptInvite);
    }
}